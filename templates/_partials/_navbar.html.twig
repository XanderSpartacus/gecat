<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav w-100">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_dashboard') }}">
                        <i class="bi bi-grid-fill"></i>
                        Tableau de bord
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_courrier_list') }}">
                        <i class="bi bi-arrow-repeat"></i>
                        Courriers
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_courrier_new') }}">
                        <i class="bi bi-envelope-fill"></i>
                        Nouveau courrier
                    </a>
                </li>
                <li class="nav-item ms-auto d-flex align-items-center">
                    <span class="navbar-text me-3 text-white-50">
                        <i class="bi bi-person-circle text-white me-1"></i>
                        {{ app.user.userIdentifier }}
                        <span class="badge bg-light text-dark ms-1" style="font-size: 0.7rem;">
                            {% if is_granted('ROLE_ADMIN') %}
                                ADMIN
                            {% elseif is_granted('ROLE_GESTIONNAIRE') %}
                                GESTIONNAIRE
                            {% else %}
                                UTILISATEUR
                            {% endif %}
                        </span>
                    </span>
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        <i class="bi bi-power"></i>
                        Déconnexion
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
